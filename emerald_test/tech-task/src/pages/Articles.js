import React from "react";

const Articles = (props) => {

    return (
        <div className="main">
            This is the Articles list, you must be logged in (assuming restricted routes are setup yet)
        </div>
    );
}

export default Articles;