import React from "react";

const Home = (props) => {
    return (
        <div className="main">
            This is home, you can this regardless of being logged in or not
        </div>
    );
}

export default Home;